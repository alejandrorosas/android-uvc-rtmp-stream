name: Test
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: docker://roviapps/android-sdk
      - name: Build
        run: gradle build
      - name: Test
        run: gradle test
